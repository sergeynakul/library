<%= form_with url: add_book_admin_group_path(group), local: true, method: :put do |form| %>
  <%= form.label :book_id, 'Title' %>
  <%= form.collection_select(:book_id, Book.can_add_in(group), :id, :title, prompt: 'Select') %>
  <%= form.submit 'Add book' %>
<% end %>