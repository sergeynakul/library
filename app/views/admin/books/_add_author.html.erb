<%= form_with url: add_author_admin_book_path(book), local: true, method: :put do |form| %>
  <%= form.label :author_id, 'Name' %>
  <%= form.collection_select(:author_id, Author.can_write(book), :id, :name, prompt: 'Select') %>
  <%= form.submit 'Add author' %>
<% end %>